# Marketing copy for create-claude-plugin

<!--
  Drafted by Phase 5.5 of the `create-claude-plugin` skill against the rubric
  at skills/create-claude-plugin/reference/marketing-copy.md.
-->

## Launch tweet

Shipped `create-claude-plugin`: a Claude plugin that helps you ship Claude plugins.

Scaffold → validate → push → submit to the official marketplace in ~20 min, all in-session. Clipboard pre-staged, browser opened at the form.

github.com/codyhxyz/create-claude-plugin

## Alt tweets

- "I built a skill in ~/.claude and had no idea how to share it." `create-claude-plugin` closes that loop — scaffold, validate, push, submit, all in one Claude Code session. ~20 minutes start to finish. github.com/codyhxyz/create-claude-plugin
- `create-claude-plugin` v0.4.0: pick up where you left off. Scaffold Monday, document Wednesday, submit Friday. `--status` tells you what's left, a SessionStart banner flags blocked phases, CLAUDE.md keeps the rules loaded across sessions. github.com/codyhxyz/create-claude-plugin
- 80% of shipping a Claude plugin is the boilerplate nobody writes: marketplace.json, the GitHub topics, the submission-form fields, the Cowork smoke-test. `create-claude-plugin` does the 80% so you can decide what the plugin should actually say. github.com/codyhxyz/create-claude-plugin

<!--
  OG card (1200×630 social-preview for Twitter / Slack / iMessage / LinkedIn):
  rendered at assets/og.png by the og-card skill. To re-generate:
      skills/og-card/scripts/render.sh marketing/og.config.mjs
  Upload to GitHub → repo Settings → Social preview → Edit (manual one-time).
-->
